syntax = "proto3";

package common;


message TokenLamports {
    uint64 amount = 1;
    uint32 decimals = 2;
}
message Decimal {
    string value = 1;
}
message TokenAddress {
    string mint = 1;
}

message UnixTimestamp {
    int64 timestamp = 1;
}
  
enum QuoteKind {
    SOL=0;
    USDC=1;
}